# Bruno .bru to Open Collection converter

## Running

`go run . -o <directory containing .bru collection> -o <output directory for Open Collection collection>`

Example
`go run . -i mycollection -o myopencollection`

> [!NOTE]
> Currently only supports HTTP and gRPC requests